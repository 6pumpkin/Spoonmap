# Walkthrough: Phase 3.1 식당 마스터 엔티티화 및 중복 방지

카카오 장소 검색 API를 연동하여 방문 일기/식당 등록 시 도로명 주소, 좌표(위/경도), 공식 카카오 Place ID, 표준 카테고리, 지역 대/소분류를 자동으로 채워주고, 다중 식별 계층을 통해 동일 식당의 중복 등록을 원천 차단했습니다.

---

## 변경 내용

### 1. 카카오 장소 검색 API 기반 자동완성 연동 (`app.js`, `index.html`, `style.css`)
- **실시간 자동완성**: 식당명 입력란에 글자를 입력하면 디바운스(220ms)를 거쳐 카카오 장소 검색(`kakao.maps.services.Places`) 및 로컬 등록 식당을 실시간 검색.
- **컴팩트 UI**: 불필요한 장황한 문구를 배제하고 `[식당명 / 카테고리 / 주소]` 2줄 구조 및 `[등록됨 · N회 방문]` 미니 뱃지만 담백하게 노출.
- **키보드 조작 지원**: 방향키(위/아래)로 추천 목록을 탐색하고 Enter로 즉시 선택 가능.
- **원클릭 자동 채움(Auto-fill)**:
  - 카카오맵 상세 URL (`place_url`)
  - 도로명 주소 및 지번 주소
  - 공식 카카오 Place ID (`place.id`)
  - 정밀 좌표 (`x`, `y`)
  - 표준 카테고리 자동 변환 매핑 (`mapKakaoCategoryToStandard`)
  - 대한민국 표준 행정구역 기반 대분류/소분류 자동 파싱 (`extractRegionFromAddress`)

### 2. 다중 식별 계층 중복 등록 방어벽 (`app.js`)
- **다단계 식별 로직 (`findExistingRestaurant`)**:
  1. `1순위`: 카카오 Place ID 일치 여부
  2. `2순위`: 카카오맵 상세 URL 내 장소 고유 번호 일치 여부
  3. `3순위`: 정규화 명칭(공백/특수문자/본점·직영점 수식어 제거) + 지역 소분류/대분류 일치 여부
  4. `4순위`: 정규화 명칭 + GPS 150m 이내 근접 여부
- **중복 방어 처리**:
  - 이미 등록된 식당인 경우 식당명이 조금 달라도(예: 띄어쓰기 차이, 본점 표기 유무) 기존 식당의 정식 명칭과 키로 자동 통합.
  - 중복 식당 카드가 새로 생성되지 않고 기존 식당의 N번째 방문 기록(다이어리/또간집)으로 누적 연결.
  - 기존 식당에 없던 좌표(x, y)와 카카오 ID, 도로명 주소를 마스터에 보강 업데이트.

### 3. 간결한 토스트 알림 및 UI 정리
- 쓸데없는 설명조 멘트를 모두 걷어내고, `"진미평양냉면" 방문 기록 저장 완료`, `"농민백암순대" 등록 완료` 등 직관적이고 담백한 문구만 노출.

---

## 검증 결과

- **구문 무결성 검증**: `check_js_syntax.ps1` 통과 (`[VALIDATION SUCCESS] app.js syntax & bracket balance verified clean!`)
- **배포 확인**: `push_github.ps1` 정상 완료 (`To https://github.com/6pumpkin/Spoonmap.git main -> main`, Commit: `f2a8730`)
