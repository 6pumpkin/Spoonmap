# 🚀 Future Implementation Plan: Kakao OAuth, Multi-User Support & Social Sharing

사용자분께서 요청하신 향후 3가지 핵심 기능(카카오톡 로그인 연동, 사용자별 기록 분리 관리, 식당 개체화 및 소셜 공유)에 대한 전용 플랜 문서입니다.

---

## 📌 Phase 1: 카카오톡 로그인 연동 (Kakao OAuth 2.0 Integration)

### 1.1 목표
- 카카오 계정 인증을 도입하여 사용자가 간편하게 로그인할 수 있도록 지원.
- 로그인한 사용자에 한해 개인 맞춤형 `DIARY`, `LIST`, `INSIGHT` 등의 데이터를 안전하게 로드.

### 1.2 주요 작업 내용
- **Kakao Javascript SDK / REST API 연동**
  - 헤더 영역에 [카카오 로그인 / 프로필 / 로그아웃] 버튼 및 유저 상태 UI 구성.
- **인증 세션 관리**
  - 로그인 토큰에 기반한 개인 뷰 접근 권한 제어.

---

## 📌 Phase 2: 다중 사용자 개인화 데이터 독립 관리 (Multi-User Data Isolation)

### 2.1 목표
- 사용자(`User ID`)별로 독립적인 식사 일기 및 식당 방문 기록을 저장/수정/삭제하도록 스코프 관리.

### 2.2 주요 작업 내용
- **사용자 스코프 기반 스키마 설계**
  - User 테이블과 UserVisitLog (방문일, 수저평점, 태그, 메모 등) 분리.
- **개인 뷰 렌더링**
  - 현재 로그인된 사용자 ID에 맞춘 `LIST`, `DIARY`, `INSIGHT` 화면 바인딩.

---

## 📌 Phase 3: 식당 개체화 & 소셜 공유 네트워크 (Restaurant Entity & Social Sharing)

### 3.1 목표
- 식당 정보를 표준 개체(Master Entity)로 통합 관리하고, 다른 사용자들과 방문 기록 및 추천 식당을 공유.

### 3.2 주요 작업 내용
- **식당 마스터 엔티티화**
  - 카카오 장소 ID 기반 마스터 식당 데이터 표준화.
- **미식 공유 피드 & 소셜 지도**
  - 다른 사용자들의 또간집 랭킹, 실시간 방문 후기 공유 기능 신설.
