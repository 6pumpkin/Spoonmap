# [Phase 3.3] 내 맛집 지도 & 위시리스트 웹 공유 링크

## 개요
내 맛집 지도와 위시리스트를 지인에게 안전하게 전달할 수 있는 **프라이빗 랜덤 코드 기반 웹 공유 링크(`?share={token}`)**를 생성하고, 링크를 열어본 친구가 내 맛집 지도를 감상하며 마음에 드는 식당을 본인 계정으로 즉시 스크랩(위시리스트 담기)할 수 있는 소셜 공유 기능을 구축합니다.

---

## 사용자 확인 및 검토 사항 (User Review Required)

> [!IMPORTANT]
> **핵심 동작 규칙 요약**:
> 1. **프라이빗 링크 보안**:
>    - `https://6pumpkin.github.io/Spoonmap/?share={랜덤토큰}` 형태로 생성되어, 링크를 전달받은 지인만 열람할 수 있습니다.
>    - 프로필 모달에서 언제든 공유를 끄거나(비활성화), 새 링크로 재발급할 수 있습니다.
> 2. **방문자(친구) 화면 구성**:
>    - 상단 안내 바: `👑 {닉네임}님의 맛집 지도 (맛집 N곳 · 위시리스트 M곳) [✕ 내 지도로 돌아가기]`
>    - 친구의 데이터는 **읽기 전용**으로 안전하게 보호되며(수정/삭제 버튼 자동 숨김), `[지도]`와 `[리스트]`, `[위시리스트]`를 자유롭게 탐색할 수 있습니다.
> 3. **원클릭 맛집 스크랩**:
>    - 식당 상세 모달에서 `[⭐ 내 위시리스트에 담기]` 버튼을 누르면 본인 계정의 위시리스트에 즉시 저장됩니다 (비로그인 시 간편 로그인 안내).

---

## 제안하는 변경 사항

### 1. UI 템플릿 및 모달 (`index.html`, `style.css`)
- **[MODIFY] [index.html](file:///d:/PUMPKIN/Spoonmap/index.html)**:
  - **헤더 프로필 영역 & 프로필 탭**:
    - `[🔗 내 맛집 지도 공유]` 버튼 추가.
  - **공유 링크 관리 모달 (`share-link-modal`)**:
    - 생성된 고유 링크 주소 표시창 + `[🔗 링크 복사]` 버튼.
    - 공유 활성화 토글(ON/OFF) + `새 링크로 재발급` 버튼.
  - **공유 모드 방문자 상단 웰컴 배너 (`shared-map-banner`)**:
    - `👑 {호스트}님의 맛집 지도` 인포 배너 및 `[내 지도로 돌아가기]` 버튼.
  - **식당 상세 팝업 액션 바**:
    - 공유 모드 방문자일 때 `[⭐ 내 위시리스트에 담기]` 버튼 동적 노출.

- **[MODIFY] [style.css](file:///d:/PUMPKIN/Spoonmap/style.css)**:
  - 상단 웰컴 배너 디자인 (고급스러운 크림/오렌지 투톤 바).
  - 공유 관리 모달 및 복사 완료 애니메이션 스타일.
  - 방문자용 스크랩 버튼 스타일.

---

### 2. 클라우드 공유 엔진 및 링크 라우팅 (`app.js`)
- **[MODIFY] [app.js](file:///d:/PUMPKIN/Spoonmap/app.js)**:
  - **공유 데이터 퍼블리시 엔진 (`publishSharedMap`)**:
    - 내 등록 맛집(`getUnifiedRestaurantData()`) 및 위시리스트(`getUserWishlist()`)를 Firestore `spoonmap_shared_maps` 컬렉션에 토큰 기반으로 발행.
    - 개인 비공개 메모는 보호하고 맛집 위치, 평점, 메뉴, 카테고리, 대표 사진 정보만 정제하여 저장.
  - **공유 링크 파라미터 감지 및 모드 전환 (`initSharedMapRoute`)**:
    - 페이지 접속 시 URL SearchParams에 `?share={token}`이 있는지 확인.
    - 토큰 유효성 검증 후 호스트의 맛집/위시리스트 데이터를 로드하여 지도 마커와 리스트 탭에 렌더링.
  - **원클릭 스크랩 핸들러 (`scrapeSharedRestaurantToWishlist`)**:
    - 호스트 식당 상세 화면에서 클릭 시 방문자의 로컬 및 Firestore `wishlist`에 즉시 추가.

---

## 검증 계획 (Verification Plan)

### 자동 검증
1. **자바스크립트 구문 무결성 검증**:
   - `powershell -ExecutionPolicy Bypass -File .\check_js_syntax.ps1` 실행하여 오류 0건 확인.

### 수동 기능 검증
1. **링크 생성 및 복사 검증**:
   - 프로필에서 `[🔗 내 맛집 지도 공유]` 클릭 ➔ 고유 토큰 생성 및 클립보드 복사 확인.
2. **공유 링크 접속(방문자 뷰) 검증**:
   - 브라우저 시크릿 창(비로그인 상태)에서 복사한 `?share={token}` 링크로 접속.
   - 상단에 `👑 {닉네임}님의 맛집 지도` 배너가 뜨고 호스트의 맛집 마커와 위시리스트가 정상 노출되는지 확인.
   - 수정/삭제 버튼이 숨겨지고 읽기 전용으로 안전하게 열리는지 확인.
3. **원클릭 스크랩 검증**:
   - 식당 상세 모달에서 `[⭐ 내 위시리스트에 담기]` 버튼 동작 확인.
4. **GitHub Pages 배포**:
   - `powershell -ExecutionPolicy Bypass -File .\push_github.ps1` 실행 및 정상 배포 확인.
